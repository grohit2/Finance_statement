# Bronze canonical columns:
#   profile_id (string), account_id (string), post_date (string),
#   description (string), amount (double), currency (string), balance (double)
# Silver adds:
#   clean_desc (string), merchant (string), category (string), is_transfer (bool),
#   txn_id (string), year (int), month (int)
